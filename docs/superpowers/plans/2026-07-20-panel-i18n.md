# Panel i18n (EN+TR) + Marka + İki Set Görsel Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Panel arayüzü + API mesajları EN/TR konuşur, marka `audiobook-ttspanel` olur, README görselleri EN/TR iki set olarak yenilenir.

**Architecture:** El yapımı hafif i18n: `lib/i18n/` altında tip-güvenli TR/EN sözlükleri; istemcide `LanguageProvider` + `useT()` (cookie `lang`), sunucuda `resolveLang(cookie, acceptLanguage)` + `tServer(req, key)`. Sayfalar dosya dosya anahtara göçer; sonra iki dilde ekran görüntüsü çekilip README'ler güncellenir.

**Tech Stack:** Next.js 15 (App Router), React 19 context, vitest, Playwright (scratchpad'de, repo'ya girmez).

**Spec:** `docs/superpowers/specs/2026-07-20-panel-i18n-design.md`
**Branch:** `feat/panel-i18n` (main'den)

**Genel kurallar (her göç görevi için):**
- Anahtar adı `alan.eylem` (`sidebar.library`, `chapter.generate`, `error.wrongPassword`). Aynı metin iki yerde geçiyorsa TEK anahtar (`common.` öneki: `common.save`, `common.cancel`, `common.loading`).
- Kod yorumları Türkçe kalır — yalnız KULLANICIYA GÖRÜNEN metin göçer (JSX metni, `title`/`aria-label`/`placeholder`/`alert`/`confirm` metinleri, API `error`/`message` alanları).
- Görev sonu doğrulama komutu (yorum satırları hariç Türkçe kalmamalı; çıktıyı gözle süz):
  `grep -nE '[çğıöşüÇĞİŞÖÜ]' <dosya> | grep -vE ':[[:space:]]*(//|/\*|\*)'`
- EN çevirileri implementer yazar; kısa, düğme-uygun, teknik terimler İngilizce endüstri diliyle (Generate, Stitch, Settings, Continue listening…). README.md'deki İngilizce terimlerle tutarlı ol (orada "Produce" yerine ne kullanıldıysa ona uy — bak: README.md Usage bölümü).
- Her görev sonunda `npx tsc --noEmit` + `npm test` yeşil.

---

### Task 1: i18n çekirdeği (TDD)

**Files:**
- Create: `lib/i18n/tr.ts`, `lib/i18n/en.ts`, `lib/i18n/index.ts`
- Test: `tests/panel/i18n.test.ts`

- [ ] **Step 1: Başarısız testleri yaz**

```ts
// tests/panel/i18n.test.ts
import { describe, expect, it } from 'vitest';
import { tr } from '@/lib/i18n/tr';
import { en } from '@/lib/i18n/en';
import { format, getDict, resolveLang } from '@/lib/i18n';

describe('i18n sözlükleri', () => {
  it('TR ve EN anahtar kümeleri birebir aynı', () => {
    expect(Object.keys(en).sort()).toEqual(Object.keys(tr).sort());
  });
  it('boş çeviri yok', () => {
    for (const d of [tr, en] as const)
      for (const [k, v] of Object.entries(d)) expect(v, k).not.toBe('');
  });
});

describe('resolveLang', () => {
  it('geçerli cookie kazanır', () => {
    expect(resolveLang('en', 'tr-TR,tr;q=0.9')).toBe('en');
    expect(resolveLang('tr', 'en-US')).toBe('tr');
  });
  it('cookie yoksa Accept-Language: tr birincilse tr', () => {
    expect(resolveLang(undefined, 'tr-TR,tr;q=0.9,en;q=0.8')).toBe('tr');
    expect(resolveLang(undefined, 'en-US,en;q=0.9')).toBe('en');
  });
  it('deforme/boş girdi → en', () => {
    expect(resolveLang(undefined, undefined)).toBe('en');
    expect(resolveLang('xx', '')).toBe('en');
  });
});

describe('format', () => {
  it('yer tutucuları doldurur', () => {
    expect(format('Merhaba {name}, {n} bölüm', { name: 'Ada', n: 3 })).toBe('Merhaba Ada, 3 bölüm');
  });
  it('bilinmeyen yer tutucu olduğu gibi kalır', () => {
    expect(format('{x} kaldı', {})).toBe('{x} kaldı');
  });
});

describe('getDict', () => {
  it('dile göre sözlük döner', () => {
    expect(getDict('tr')).toBe(tr);
    expect(getDict('en')).toBe(en);
  });
});
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

Run: `npx vitest run tests/panel/i18n.test.ts`
Expected: FAIL (modüller yok).

- [ ] **Step 3: Modülleri yaz**

```ts
// lib/i18n/tr.ts — TEK doğruluk kaynağı; anahtarlar buradan türer.
// Başlangıçta çekirdek anahtarlar; göç görevleri anahtar ekledikçe büyür.
export const tr = {
  'common.loading': 'Yükleniyor…',
  'common.save': 'Kaydet',
  'common.cancel': 'Vazgeç',
  'common.delete': 'Sil',
  'common.confirmAgain': 'Emin misin?',
  'settings.language': 'Dil / Language',
} as const;
export type MessageKey = keyof typeof tr;
```

```ts
// lib/i18n/en.ts
import type { MessageKey } from './tr';
export const en: Record<MessageKey, string> = {
  'common.loading': 'Loading…',
  'common.save': 'Save',
  'common.cancel': 'Cancel',
  'common.delete': 'Delete',
  'common.confirmAgain': 'Are you sure?',
  'settings.language': 'Language / Dil',
};
```

```ts
// lib/i18n/index.ts
import { tr, type MessageKey } from './tr';
import { en } from './en';

export type Lang = 'tr' | 'en';
export type { MessageKey };
export const LANG_COOKIE = 'lang';

export function getDict(lang: Lang): Record<MessageKey, string> {
  return lang === 'tr' ? tr : en;
}

// Cookie geçerliyse o; değilse Accept-Language'ın EN YÜKSEK öncelikli dili tr ise tr; aksi halde en.
export function resolveLang(cookieValue: string | undefined, acceptLanguage: string | undefined): Lang {
  if (cookieValue === 'tr' || cookieValue === 'en') return cookieValue;
  const first = (acceptLanguage ?? '')
    .split(',')
    .map((p) => {
      const [tag, ...params] = p.trim().split(';');
      const q = Number(params.find((x) => x.trim().startsWith('q='))?.split('=')[1] ?? '1');
      return { tag: tag.trim().toLowerCase(), q: Number.isFinite(q) ? q : 0 };
    })
    .filter((x) => x.tag)
    .sort((a, b) => b.q - a.q)[0];
  return first?.tag.startsWith('tr') ? 'tr' : 'en';
}

export function format(msg: string, params: Record<string, string | number>): string {
  return msg.replace(/\{(\w+)\}/g, (m, k) => (k in params ? String(params[k]) : m));
}
```

- [ ] **Step 4: Testler yeşil**

Run: `npx vitest run tests/panel/i18n.test.ts` → PASS. Ardından `npm test` (tümü) → PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/i18n tests/panel/i18n.test.ts
git commit -m "feat(i18n): tip-güvenli TR/EN sözlük çekirdeği + resolveLang/format"
```

---

### Task 2: LanguageProvider + layout kablolama + marka

**Files:**
- Create: `lib/ui/LanguageProvider.tsx`
- Modify: `app/layout.tsx` (tamamı aşağıda)
- Modify: `public/manifest.webmanifest` (`name` ve `short_name` → `"audiobook-ttspanel"`)

- [ ] **Step 1: LanguageProvider'ı yaz**

```tsx
// lib/ui/LanguageProvider.tsx
'use client';
import { createContext, useCallback, useContext, useState, type ReactNode } from 'react';
import { format, getDict, LANG_COOKIE, type Lang, type MessageKey } from '@/lib/i18n';

const Ctx = createContext<{ lang: Lang; setLang: (l: Lang) => void } | null>(null);

export function LanguageProvider({ initialLang, children }: { initialLang: Lang; children: ReactNode }) {
  const [lang, setLangState] = useState<Lang>(initialLang);
  const setLang = useCallback((l: Lang) => {
    document.cookie = `${LANG_COOKIE}=${l}; path=/; max-age=31536000; SameSite=Lax`;
    setLangState(l);
  }, []);
  return <Ctx.Provider value={{ lang, setLang }}>{children}</Ctx.Provider>;
}

export function useLang() {
  const v = useContext(Ctx);
  if (!v) throw new Error('useLang: LanguageProvider yok');
  return v;
}

export function useT() {
  const { lang } = useLang();
  const dict = getDict(lang);
  return useCallback(
    (key: MessageKey, params?: Record<string, string | number>) =>
      params ? format(dict[key], params) : dict[key],
    [dict],
  );
}
```

- [ ] **Step 2: layout'u async yapıp dili çöz, provider'ı tak, markayı değiştir**

`app/layout.tsx`'in tamamı (mevcut import/font/viewport satırları korunur; değişen kısımlar: `cookies`/`headers` importu, async fonksiyon, `metadata.title`, `<html lang>`, `LanguageProvider`, marka metni):

```tsx
import './globals.css';
import Link from 'next/link';
import type { ReactNode } from 'react';
import { cookies, headers } from 'next/headers';
import { JetBrains_Mono, Manrope } from 'next/font/google';
import { LANG_COOKIE, resolveLang } from '@/lib/i18n';
import { LanguageProvider } from '@/lib/ui/LanguageProvider';
import { LogoutButton } from '@/lib/ui/LogoutButton';
import { Sidebar } from '@/lib/ui/Sidebar';
import { RegisterSw } from '@/lib/ui/player/RegisterSw';
import { PlayerProvider } from '@/lib/ui/player/PlayerProvider';
import { PlayerBar } from '@/lib/ui/player/PlayerBar';

const manrope = Manrope({ subsets: ['latin', 'latin-ext'], variable: '--font-manrope' });
const jbmono = JetBrains_Mono({ subsets: ['latin', 'latin-ext'], variable: '--font-jbmono' });

export const metadata = { title: 'audiobook-ttspanel', manifest: '/manifest.webmanifest' };
export const viewport = { themeColor: '#0f1115' };

export default async function RootLayout({ children }: { children: ReactNode }) {
  const [cookieStore, headerStore] = await Promise.all([cookies(), headers()]);
  const lang = resolveLang(cookieStore.get(LANG_COOKIE)?.value, headerStore.get('accept-language') ?? undefined);
  return (
    <html lang={lang} className={`${manrope.variable} ${jbmono.variable}`}>
      <body>
        <RegisterSw />
        <LanguageProvider initialLang={lang}>
          <PlayerProvider>
            <header className="topbar">
              <Link href="/" className="brand">
                {/* Dalga-formu marka işareti */}
                <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
                  <rect x="1" y="6" width="2.5" height="6" rx="1.25" fill="currentColor" />
                  <rect x="5" y="3" width="2.5" height="12" rx="1.25" fill="currentColor" />
                  <rect x="9" y="1" width="2.5" height="16" rx="1.25" fill="currentColor" opacity="0.85" />
                  <rect x="13" y="5" width="2.5" height="8" rx="1.25" fill="currentColor" />
                </svg>
                audiobook-ttspanel
              </Link>
              <span className="spacer" />
              <LogoutButton />
            </header>
            <div className="shell">
              <Sidebar />
              <main className="container">{children}</main>
            </div>
            <PlayerBar />
          </PlayerProvider>
        </LanguageProvider>
      </body>
    </html>
  );
}
```

`public/manifest.webmanifest`: `"name"` ve `"short_name"` değerlerini `"audiobook-ttspanel"` yap (başka alanlara dokunma).

- [ ] **Step 3: Doğrula**

Run: `npx tsc --noEmit` → hatasız; `npm test` → PASS.
Run: `npm run dev` arka planda → `curl -s -H 'Accept-Language: en-US' localhost:3000/login | grep -o '<html lang="en"'` → eşleşme; `curl -s -H 'Accept-Language: tr-TR' ...` → `lang="tr"`; cookie öncelik testi: `curl -s -H 'Cookie: lang=en' -H 'Accept-Language: tr-TR' ...` → `lang="en"`. Sunucuyu kapat.

- [ ] **Step 4: Commit**

```bash
git add lib/ui/LanguageProvider.tsx app/layout.tsx public/manifest.webmanifest
git commit -m "feat(i18n): LanguageProvider + layout dil çözümü; marka audiobook-ttspanel"
```

---

### Task 3: Sidebar göçü + TR/EN kısayolu

**Files:**
- Modify: `lib/ui/Sidebar.tsx`
- Modify: `lib/i18n/tr.ts`, `lib/i18n/en.ts` (yeni anahtarlar)

- [ ] **Step 1: Anahtarları ekle**

`tr.ts`'e ekle (EN karşılıklarını `en.ts`'e — çeviriler: Library, New project, Settings, Manage, Library menu):

```ts
  'sidebar.library': 'Kütüphane',
  'sidebar.newProject': 'Yeni proje',
  'sidebar.settings': 'Ayarlar',
  'sidebar.manage': 'Yönet',
  'sidebar.menu': 'Kütüphane menüsü',
```

- [ ] **Step 2: Sidebar'ı göçür + dil kısayolu ekle**

`Sidebar.tsx` değişiklikleri: `import { useLang, useT } from './LanguageProvider';` ekle; fonksiyon başına `const t = useT(); const { lang, setLang } = useLang();`. Metin değişimleri: `aria-label="Kütüphane menüsü"`/`title="Kütüphane"` → `aria-label={t('sidebar.menu')} title={t('sidebar.library')}`; `aria-label="Kütüphane"` (nav) → `{t('sidebar.library')}`; `Yükleniyor…` → `{t('common.loading')}`; `Yönet` → `{t('sidebar.manage')}`; `Kütüphane` → `{t('sidebar.library')}`; `Yeni proje` → `{t('sidebar.newProject')}`; `Ayarlar` → `{t('sidebar.settings')}`. Nav kapanışından önce (Ayarlar linkinin altına) kısayol:

```tsx
          {tree !== null && (
            <div className="side-lang">
              {(['tr', 'en'] as const).map((l) => (
                <button key={l} className={l === lang ? 'lang-btn on' : 'lang-btn'} onClick={() => setLang(l)}>
                  {l.toUpperCase()}
                </button>
              ))}
            </div>
          )}
```

`app/globals.css` sonuna (mevcut token/renk değişkenlerini kullanarak) küçük stil ekle:

```css
/* Sidebar dil kısayolu */
.side-lang { display: flex; gap: 4px; padding: 8px 10px; }
.lang-btn { font-size: 11px; padding: 2px 8px; border-radius: 6px; opacity: 0.55; background: none; }
.lang-btn.on { opacity: 1; outline: 1px solid var(--line, #333); }
```

(`--line` yoksa globals.css'te tanımlı çizgi/kenar değişkeni neyse onu kullan; uydurma renk girme.)

- [ ] **Step 3: Doğrula + commit**

`npx tsc --noEmit` + `npm test` → PASS. Görev doğrulama grep'i (genel kurallar) Sidebar.tsx'te yalnız yorum satırları döndürmeli.

```bash
git add lib/ui/Sidebar.tsx lib/i18n app/globals.css
git commit -m "feat(i18n): sidebar göçü + TR/EN kısayolu"
```

---

### Task 4: Küçük sayfalar göçü (ana sayfa, login, proje yönetimi, ortak bileşenler)

**Files:**
- Modify: `app/page.tsx`, `app/login/page.tsx`, `app/projects/[id]/page.tsx`, `lib/ui/LogoutButton.tsx`, `lib/ui/ConfirmButton.tsx`
- Modify: `lib/i18n/tr.ts`, `lib/i18n/en.ts`

- [ ] **Step 1: Envanter çıkar**

Run: `grep -nE '[çğıöşüÇĞİŞÖÜ]' app/page.tsx app/login/page.tsx 'app/projects/[id]/page.tsx' lib/ui/LogoutButton.tsx lib/ui/ConfirmButton.tsx | grep -vE ':[[:space:]]*(//|/\*|\*)'`
Her görünen metin için `home.*` / `login.*` / `project.*` / `common.*` anahtarı planla (aynı metin → ortak anahtar).

- [ ] **Step 2: Göçür**

Kalıp (her dosyada): `'use client'` zaten var → `import { useT } from '@/lib/ui/LanguageProvider';` + `const t = useT();`. Örnek dönüşümler:

```tsx
// ÖNCE:  <button className="danger" onClick={click}>Emin misin?</button>
// SONRA (ConfirmButton içinde):
const t = useT();
...
<button className="danger" onClick={click}>{t('common.confirmAgain')}</button>
// ariaLabel prop'unun varsayılanı 'Sil' string'i yerine: ariaLabel?: string ve
// kullanım yerinde ariaLabel ?? t('common.delete')
```

Dinamik metinler `format` yer tutucusuyla: ör. `` `${n} bölüm` `` → anahtar `'project.chapterCount': '{n} bölüm'` / `'{n} chapters'` ve `t('project.chapterCount', { n })`.

- [ ] **Step 3: Doğrula + commit**

Envanter grep'i tüm 5 dosyada yalnız yorum döndürür; `npx tsc --noEmit` + `npm test` PASS. Dev sunucuyu `lang=en` cookie ile açıp `/`, `/login`, `/projects/<id>` sayfalarında Türkçe kalıntı olmadığını gözle kontrol et (curl ile HTML'de `[çğıöşü]` grep'i de yeterli — dinamik içerik hariç).

```bash
git add app/page.tsx app/login/page.tsx 'app/projects/[id]/page.tsx' lib/ui/LogoutButton.tsx lib/ui/ConfirmButton.tsx lib/i18n
git commit -m "feat(i18n): ana sayfa, login, proje yönetimi ve ortak bileşen göçü"
```

---

### Task 5: Bölüm (stüdyo) sayfası göçü — en büyük dosya

**Files:**
- Modify: `app/chapters/[id]/page.tsx` (430 satır)
- Modify: `lib/i18n/tr.ts`, `lib/i18n/en.ts`

- [ ] **Step 1: Envanter** — Task 4'teki grep aynı dosyaya; ~60-80 metin beklenir (adımlar "01 Metin/02 Script/03 Üretim", Üret, Birleştir, durum rozetleri, preflight satırları, segment tablosu başlıkları, hata mesajları). Anahtar öneki `chapter.*`.

- [ ] **Step 2: Göçür** — Task 4 kalıbı. Dikkat: EN düğme adları README.md Usage bölümüyle tutarlı olsun ("Generate script (LLM)", "Produce", "Stitch"). Durum değerleri (`draft`/`scripted`/`voiced`/`done`) VERİ — çevrilmez; onları gösteren etiketler çevrilir.

- [ ] **Step 3: Doğrula + commit** — grep temiz, `npx tsc --noEmit` + `npm test` PASS.

```bash
git add 'app/chapters/[id]/page.tsx' lib/i18n
git commit -m "feat(i18n): bölüm/stüdyo sayfası göçü"
```

---

### Task 6: Kütüphane + oynatıcı göçü

**Files:**
- Modify: `app/library/page.tsx`, `lib/ui/player/PlayerBar.tsx`, `lib/ui/player/PlayerProvider.tsx`
- Modify: `lib/i18n/tr.ts`, `lib/i18n/en.ts`

- [ ] **Step 1: Envanter** — aynı grep üç dosyaya. Anahtar önekleri `library.*`, `player.*` ("Devam et" → `library.continue` = "Continue listening", indir/sil, hız etiketi, ±15/30 aria'ları, MediaSession metinleri varsa).
- [ ] **Step 2: Göçür** — Task 4 kalıbı. `PlayerProvider` context sağlıyorsa `useT`'yi provider İÇİNDEKİ bileşenlerde kullan — `LanguageProvider` layout'ta `PlayerProvider`'ın DIŞINDA olduğundan sorun yok.
- [ ] **Step 3: Doğrula + commit**

```bash
git add app/library/page.tsx lib/ui/player lib/i18n
git commit -m "feat(i18n): kütüphane ve oynatıcı göçü"
```

---

### Task 7: Ayarlar sayfası göçü + dil seçici

**Files:**
- Modify: `app/settings/page.tsx` (244 satır)
- Modify: `lib/i18n/tr.ts`, `lib/i18n/en.ts`

- [ ] **Step 1: Envanter** — anahtar öneki `settings.*`.
- [ ] **Step 2: Göçür + seçici ekle** — sayfanın üstüne (mevcut bölüm/kart düzenini izleyerek) dil bölümü:

```tsx
const { lang, setLang } = useLang();
...
<section className="card">
  <h2>{t('settings.language')}</h2>
  <div className="row">
    <label><input type="radio" name="lang" checked={lang === 'tr'} onChange={() => setLang('tr')} /> Türkçe</label>
    <label><input type="radio" name="lang" checked={lang === 'en'} onChange={() => setLang('en')} /> English</label>
  </div>
</section>
```

("Türkçe"/"English" özel ad — çevrilmez. `card`/`row` sınıfları mevcut settings sayfasında ne kullanılıyorsa ona uydurulur; yeni sınıf uydurma.)

- [ ] **Step 3: Doğrula + commit** — grep temiz, tsc + test PASS; dev'de seçiciyle dil değişiminin yenilemesiz çalıştığını gözle doğrula.

```bash
git add app/settings/page.tsx lib/i18n
git commit -m "feat(i18n): ayarlar göçü + dil seçici"
```

---

### Task 8: API mesajları (tServer) + test güncellemeleri

**Files:**
- Create: `lib/i18n/server.ts`
- Modify: Türkçe metin içeren `app/api/**/route.ts` dosyaları (envanterle bul) ve kullanıcıya akan mesaj üreten `lib/services/*` noktaları
- Test: mevcut `tests/panel/*.test.ts` beklentileri + `tests/panel/i18n.test.ts`'e tServer testi

- [ ] **Step 1: tServer'ı yaz + test**

```ts
// lib/i18n/server.ts
import type { NextRequest } from 'next/server';
import { format, getDict, LANG_COOKIE, resolveLang, type MessageKey } from './index';

export function langFromRequest(req: NextRequest) {
  return resolveLang(req.cookies.get(LANG_COOKIE)?.value, req.headers.get('accept-language') ?? undefined);
}

export function tServer(req: NextRequest, key: MessageKey, params?: Record<string, string | number>): string {
  const msg = getDict(langFromRequest(req))[key];
  return params ? format(msg, params) : msg;
}
```

`tests/panel/i18n.test.ts`'e ekle (NextRequest yerine uyumlu sahte obje kullan):

```ts
import { tServer } from '@/lib/i18n/server';

describe('tServer', () => {
  const fakeReq = (cookie?: string, accept?: string) =>
    ({
      cookies: { get: (n: string) => (cookie && n === 'lang' ? { value: cookie } : undefined) },
      headers: new Headers(accept ? { 'accept-language': accept } : {}),
    }) as unknown as import('next/server').NextRequest;

  it('cookie diline göre mesaj döner', () => {
    expect(tServer(fakeReq('en'), 'common.loading')).toBe('Loading…');
    expect(tServer(fakeReq('tr'), 'common.loading')).toBe('Yükleniyor…');
  });
  it('cookie yoksa accept-language', () => {
    expect(tServer(fakeReq(undefined, 'tr-TR'), 'common.loading')).toBe('Yükleniyor…');
  });
});
```

- [ ] **Step 2: Rotaları envanterle ve göçür**

Run: `grep -rnE '[çğıöşüÇĞİŞÖÜ]' app/api lib/services lib/llm --include='*.ts' | grep -vE ':[[:space:]]*(//|/\*|\*)'`
Her kullanıcıya dönen `error`/`message` alanı `tServer(req, 'error.…')` olur (anahtar öneki `error.*` / `job.*`). Örnek:

```ts
// ÖNCE: return NextResponse.json({ error: 'Hatalı şifre' }, { status: 401 });
// SONRA: return NextResponse.json({ error: tServer(req, 'error.wrongPassword') }, { status: 401 });
```

`req` erişimi olmayan katmanlarda (worker'ın DB'ye yazdığı metin — producer vb.): spec kararı gereği üretim anındaki dille yazılır; iş kuyruğuna dil `resolveLang` ile İSTEK anında karar verilip parametre olarak taşınır YALNIZCA halihazırda metin üretilen noktalara (yeni altyapı kurma; mevcut fonksiyon imzasına `lang: Lang` parametresi eklemek yeterli). Log/iç hata metinleri (kullanıcı görmüyorsa) Türkçe kalabilir.

- [ ] **Step 3: Türkçe metin bekleyen testleri güncelle**

Run: `grep -rlnE '[çğıöşüÇĞİŞÖÜ]' tests/panel` — beklentisi değişen testlerde: test isteği cookie'siz gidiyorsa artık EN mesaj döner (resolveLang varsayılanı en) → beklentiyi EN metnine çevir YA DA isteğe `Cookie: lang=tr` ekleyip TR bekle. Testin ne doğruladığına göre karar ver; her iki dilde davranışı sabitleyen en az bir test bırak (ör. login hatası TR cookie ile TR, cookiesiz EN).

- [ ] **Step 4: Doğrula + commit** — envanter grep'i yalnız yorum/iç-log döndürür; `npx tsc --noEmit` + `npm test` PASS.

```bash
git add lib/i18n/server.ts app/api lib/services tests/panel
git commit -m "feat(i18n): API mesajları tServer'a göçtü; testler iki dile göre güncellendi"
```

---

### Task 9: Uçtan uca EN doğrulaması

- [ ] **Step 1:** `npm test` + `npx tsc --noEmit` → tümü PASS.
- [ ] **Step 2:** Mock env ile dev sunucu (Task 10'daki komut, geçici DATA_DIR). `lang=en` cookie'li curl'lerle `/`, `/login`, `/library`, `/settings` HTML'inde `[çğıöşü]` kalıntısı ara (dinamik kullanıcı verisi hariç beklenen: 0; ayrıca `audiobook-ttspanel` markası görünmeli). `lang=tr` ile aynı sayfalar Türkçe.
- [ ] **Step 3:** Kalıntı bulunursa ilgili göç görevine dönüp düzelt; temizse sunucuyu kapat, ara commit gerekmiyorsa geç.

---

### Task 10: İki set ekran görüntüsü (EN + TR)

**Files:**
- Create: `docs/screenshots/en/studio.png`, `docs/screenshots/en/library.png`, `docs/screenshots/tr/studio.png`, `docs/screenshots/tr/library.png`
- Delete: `docs/screenshots/studio.png`, `docs/screenshots/library.png` (Task 11'de git rm)

Yöntem, önceki turun aynısı (bkz. `docs/superpowers/plans/2026-07-20-public-repo-readme.md` Task 2 — sunucu başlatma, API seed akışı, Playwright kurulumu scratchpad'de). Farklar:

- [ ] **Step 1:** Mock sunucu + geçici DATA_DIR başlat. TR içerik seed et (önceki turdaki "Kayıp Fener"/"Gölgede Kalan Şehir" metinleri — plan dosyasında mevcut) ve EK OLARAK İngilizce iki proje seed et. İngilizce örnek kurgu (özgün, bu plan için yazıldı):
  - Proje "The Salt Tower", bölüm "Chapter 1 — The Landing": `The ferry cut its engine a hundred meters from the island, and the silence that followed felt heavier than the fog.\n\n"You're the new keeper?" the old woman asked, not looking up from her nets.\n\n"For the season," Ilya said. "Maybe longer."\n\n"That's what the last one said." She finally raised her eyes toward the tower on the cliff. "The light works. It's the stairs that don't."`
  - Proje "A Quiet Inventory", bölüm "Chapter 1 — Shelf by Shelf" (library'de ikinci seri görünsün diye): `Marta counted the jars twice before admitting one was missing.\n\n"Nobody steals plum jam," her brother said from the doorway.\n\n"Somebody does now," she answered, and wrote it in the ledger with a steady hand.`
  - EN bölümlerden birine dinleme ilerlemesi PUT'la ("Continue listening" kartı dolsun).
- [ ] **Step 2:** Playwright çekimi iki tur: sayfa cookie'si `lang=tr` ile TR sayfaları (TR bölüm/`/library`) → `docs/screenshots/tr/*.png`; `lang=en` ile EN bölüm sayfası + `/library` → `docs/screenshots/en/*.png`. Cookie: `context.addCookies([{ name: 'lang', value: 'en', url: 'http://localhost:3000' }])`. 1440×900 @2x, dev overlay gizli. NOT: `/library` her iki dilde de TÜM serileri gösterir (TR+EN karışık liste kaçınılmaz — kabul; istersen library çekiminden önce yalnız o dilin projelerini seed edip iki ayrı DATA_DIR turu yap: temiz ama yavaş. Temiz yolu seç: her dil için ayrı geçici DATA_DIR + ayrı seed + ayrı çekim).
- [ ] **Step 3:** 4 PNG'yi Read ile gözle doğrula: doğru dil, dolu segment tablosu, "Continue listening"/"Devam et" kartı, marka `audiobook-ttspanel`, hata/boş ekran yok.
- [ ] **Step 4:** Sunucuyu kapat; commit:

```bash
git add docs/screenshots/en docs/screenshots/tr
git commit -m "docs: EN+TR panel ekran görüntüleri (i18n sonrası, yeni marka)"
```

---

### Task 11: README görsel yolları (iki dosya birden) + eski PNG temizliği

**Files:**
- Modify: `README.md` (2 img src: `docs/screenshots/studio.png` → `docs/screenshots/en/studio.png`, `docs/screenshots/library.png` → `docs/screenshots/en/library.png`)
- Modify: `README.tr.md` (aynı iki src → `docs/screenshots/tr/...`)
- Delete: `git rm docs/screenshots/studio.png docs/screenshots/library.png`

- [ ] **Step 1:** İki README'de src'leri güncelle; ayrıca README'lerdeki "Panel arayüzü şimdilik yalnızca Türkçe / Turkish-only" kısıt maddesini kaldırıp yerine tek cümle: EN "The panel UI speaks English and Turkish (auto-detected, switchable in Settings)." / TR "Panel arayüzü İngilizce ve Türkçe konuşur (otomatik algılanır, Ayarlar'dan değiştirilebilir)." — Features bölümüne taşımak yerine Known limitations'tan çıkarıp Features > Studio altına ekle (iki dosyada da aynı konuma).
- [ ] **Step 2:** `git rm` eski iki PNG. Link kontrolü:

```bash
grep -ohE 'src="[^"]+"' README.md README.tr.md | sed 's/src="//;s/"//' | sort -u | xargs ls
```

- [ ] **Step 3:** Commit:

```bash
git add README.md README.tr.md
git commit -m "docs: README görselleri EN/TR setlerine bağlandı; i18n kısıt notu güncellendi"
```

---

### Task 12: Son doğrulama

- [ ] **Step 1:** `npm test` → 37+ dosya PASS (i18n testleri dahil); `npx tsc --noEmit` → temiz.
- [ ] **Step 2:** `npm run build` → üretim derlemesi hatasız (manifest/SW/i18n etkileşimi için).
- [ ] **Step 3:** Göreli link + görsel bütünlüğü (Task 11 Step 2 komutu + markdown linkleri: `grep -oE '\]\(([^)#]+)' README.md README.tr.md | sed 's/.*(//' | grep -v '^http' | sort -u | xargs ls`).
- [ ] **Step 4:** Kullanıcıya bitirme seçenekleri (merge+push onayı) — finishing-a-development-branch akışı.
